# Colyseus server URL for a deployed build. Unset in local dev, which falls back to ws://localhost:2567.
# Vite inlines this at build time, so changing it on the host requires a rebuild.
VITE_SERVER_URL=wss://ougi-arena-server.onrender.com
